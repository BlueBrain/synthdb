default:  # this will be applied on all mtypes if corresponding key is not present below
MOBgr_sGC:
    basal_dendrite:
        extent_to_target:
            layer: L2
            fraction: 0.5
MOBgr_dGC:
    basal_dendrite:
        extent_to_target:
            layer: L2
            fraction: 0.15
MOBmi_MC:
    apical_dendrite:
        extent_to_target:
            layer: L1
            fraction: 0.8
MOBopl_mTC:
    apical_dendrite:
        extent_to_target:
            layer: L1
            fraction: 0.8
