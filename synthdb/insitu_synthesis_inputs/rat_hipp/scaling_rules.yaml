default:  # this will be applied on all mtypes if corresponding key is not present below
    apical_dendrite:
        hard_limit_max:
            layer: SLM
            fraction: 0.99
    basal_dendrite:
        hard_limit_max:
            layer: SLM
            fraction: 0.99

SP_PC:
    apical_dendrite:
        hard_limit_min:
            layer: SLM
            fraction: 0.5
        extent_to_target:
            layer: SLM
            fraction: 0.9
