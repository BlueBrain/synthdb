default:  # this will be applied on all mtypes if corresponding key is not present below

CBXgr_GoC:
    apical_dendrite:
        hard_limit_max:
            layer: L1
            fraction: 0.90
        hard_limit_min:
            layer: L1
            fraction: 0.1
        extent_to_target:
            layer: L1
            fraction: 0.8
